name: temply
description: Command line application for generating project default structures and classes
version: 0.0.1

executables:
  temply: main

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  args: ^1.5.0
  interact: ^1.1.0
  yaml: ^2.2.1

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
